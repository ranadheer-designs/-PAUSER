/**
 * @pauser/ui
 *
 * Design system components for Pauser.
 * These components are pure presentation - no business logic.
 *
 * @example
 * ```tsx
 * import { Button, Card, Modal } from '@pauser/ui';
 * import '@pauser/ui/styles/globals.css';
 * ```
 */

// Components
export { Button, Card, Modal } from './components';
export type { ButtonProps, ButtonVariant, ButtonSize, CardProps, ModalProps } from './components';
