# supabase/config.toml (if using local dev) or project settings

# 1. Supabase Configuration
# Found in Supabase Dashboard -> Project Settings -> API
NEXT_PUBLIC_SUPABASE_URL="https://your-project-id.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# [SERVER ONLY] Service Role Key - Bypass RLS
# WARNING: NEVER expose this to the client/browser
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# 2. AI Model Configuration (Optional for now)
# For future Edge Functions usage
OPENAI_API_KEY=""
GEMINI_API_KEY=""

# 3. Application Settings
# Base URL for the web app (DeepFocus)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
